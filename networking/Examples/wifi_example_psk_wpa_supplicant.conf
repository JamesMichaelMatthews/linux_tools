
ctrl_interface=/var/run/wpa_supplicant

network={
	ssid="your_ssid"
        key_mgmt=WPA-PSK
	#psk="your_password_string" #optional, instead of password hash
        #
        #A hash is safer, especially for valuable passwords.
        #   But it can still be copied, for this ssid, of course.
        #Generate the hash below using:
        #  wpa_passphrase your_ssid your_password_string
	psk=your_password_hash
}

